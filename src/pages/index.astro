---
import { Debug } from "astro:components";
import {
    generativeQueryWithId,
    generativeQueryWithoutId,
    generativeQueryWithPromptInFields,
} from "@src/weaviate";

const responseWithId = await generativeQueryWithId();
const responseWithoutId = await generativeQueryWithoutId();
const responseWithPromptInFields = await generativeQueryWithPromptInFields();
---

<Debug {responseWithId} />
<Debug {responseWithoutId} />
<Debug {responseWithPromptInFields} />
